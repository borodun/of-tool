apiVersion: v1
kind: Service
metadata:
  name: cpu-usage
  labels:
    app: cpu-usage
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30001
    protocol: TCP
    name: http
  selector:
    app: cpu-usage
